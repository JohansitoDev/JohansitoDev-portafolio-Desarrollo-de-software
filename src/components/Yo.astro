---
const { name, title } = Astro.props;
---
<div class="bg-blue-950 p-8 flex flex-col md:flex-row items-center md:items-start justify-between">
  <div class="flex-1 text-center md:text-left mb-4 md:mb-0">
    <h1 class="text-white text-5xl font-bold">{name}</h1>
    <p class="text-gray-300 text-lg mt-2">{title}</p>
  </div>
  <div class="flex-shrink-0 w-48 h-48 rounded-full overflow-hidden border-4 border-white shadow-lg">
    <img src="/img/profile.png" alt="Foto de perfil de Johan Mancebo" class="w-full h-full object-cover" />
  </div>
</div>