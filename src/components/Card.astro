---
const { imageUrl, text, reverse } = Astro.props;
---
<div class="flex flex-col items-center justify-center p-4" class:list={[{ 'md:flex-row-reverse': reverse }, 'md:flex-row']}>
  <div class="w-full md:w-2/3">
    <img src={imageUrl} alt={text} class="rounded-lg w-full h-auto" />
  </div>
  <div class="w-full md:w-1/3 p-4 text-white text-center md:text-left">
    <p class="text-lg font-semibold">{text}</p>
  </div>
</div>